<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidLessons" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidLessons" parent="Base.Theme.AndroidLessons" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#2D551D</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_insert</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidLessons</item>
    </style>
</resources>